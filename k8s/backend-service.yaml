apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: agromarket
  labels:
    app.kubernetes.io/name: agromarket
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: agromarket
    app.kubernetes.io/component: backend
  ports:
    - name: http
      port: 8000
      targetPort: http

